# @react-three/fiber v9 lists expo / expo-gl / expo-asset as *optional* peer
# deps (for React Native). npm 11's strict peer resolution tries to install them
# anyway and hits a version conflict. We are a web-only app, so tell npm to use
# the looser (npm 6-style) peer resolution.
legacy-peer-deps=true
