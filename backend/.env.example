# Set this in your deployment platform's environment settings (this file
# isn't auto-loaded - main.py reads os.environ directly, no dotenv).
# Left unset, the API only allows the Vite dev server's origin.
ALLOWED_ORIGINS=https://your-frontend-host.example.com
